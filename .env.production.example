# Database (PostgreSQL en Easypanel)
DATABASE_URL="postgresql://billtracky_user:PASSWORD@billtracky-db:5432/billtracky"

# NextAuth
NEXTAUTH_URL="https://billtracky.tudominio.com"
NEXTAUTH_SECRET="[GENERA_CON: openssl rand -base64 32]"

# Node Environment
NODE_ENV="production"

# Optional: Google AI (para Genkit)
GOOGLE_API_KEY="your-google-api-key"

# Optional: File Uploads
UPLOAD_DIR="./public/uploads"
MAX_FILE_SIZE="5242880"

# Optional: Email (si quieres notificaciones)
EMAIL_FROM="noreply@tudominio.com"
SMTP_HOST="smtp.tudominio.com"
SMTP_PORT="587"
SMTP_USER=""
SMTP_PASSWORD=""
